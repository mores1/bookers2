<div>

  <h3>User info</h3>
  <%= image_tag user.get_profile_image(100,100) %>
  <h3>name</h3>
  <%= user.name %>
  <h3>introduction</h3>
  <%= user.introduction %>
  <!--% if user.id == current_user.id %>-->
    <%= link_to edit_user_path(user), class: "btn btn-denger", method: :get do %>
      <i class="fas fa-user-cog"></i>
    <% end %>
  <!--% else %>-->
    <!--%= link_to user_path(current_user.id), class: "btn btn-denger", method: :get do %>-->
    <!--  <i class="fas fa-user-cog"></i>-->
    <!--% end %>-->
  <!--% end %>-->
</div>
